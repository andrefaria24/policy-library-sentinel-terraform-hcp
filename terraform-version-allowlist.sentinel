# Ensures only allowed Terraform versions are utilized during Terraform runs.

import "tfplan/v2" as tfplan

param tf_allowed_versions default ["1.10.0"]

main = rule {
	tfplan.terraform_version in tf_allowed_versions
}